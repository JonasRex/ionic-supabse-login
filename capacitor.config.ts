import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'login-project',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
